# 外部ドキュメントレビュー受領と処置（2026-07-13）

公開ドキュメント一式（README・llms.txt・spec 全 7 章・reference 全 28 ページ・stdlib 4 篇・impl/README・
design 44 ADR ほか・Pages 構成）への外部レビューを設計者経由で受領した。観点は「分かりやすさ・構成／
技術的正確さ・一貫性／網羅性・拾い漏れ／文章表現・誤字脱字」。本ページは指摘と処置の記録（原文は
設計者の手元。指摘の行番号・事実は全数をソースで再検証してから処置した）。

**総評（レビュー）**: ドキュメント本体（spec・reference・stdlib）に HIGH の誤り・矛盾なし。リンク健全性
107 ファイル全解決・用語規律（廃止語の誤残存なし）・ADR 追跡可能性・実行例の算術正しさを実測で確認、
とのこと。指摘は「維持主張（ステータス）が崩れている箇所」と「新規読者の導線」に集中した。

## 指摘と処置（重要度順・レビューの記号を踏襲）

| # | 指摘 | 検証 | 処置 |
|---|---|---|---|
| A | 「残る仮称は 1 語」の中核主張に対し構文ドラフトが「二語（shiftBoundary・nonWorking）」と記載（矛盾） | 実在 | draft §4 を「一語」に修正（nonWorking は F51 で確定済み）。**検査 7 を常設**（下記） |
| B | テスト数が入口間で不一致（README=364・llms.txt/en=363） | 実在 | 4 入口を実測値に統一。**検査 8 を常設** |
| C | draft §3 の stride が ADR-38 前の「軸相対」のまま（同一箇条内で自己矛盾） | 実在 | 入力相対に修正 |
| D | domain-model の値型が「将来の文字列」のまま・時点欠落（ADR-32/43 未反映） | 実在 | 「文字列〈ADR-32〉・時点〈ADR-43〉」に更新 |
| E | `of:` が first.md のみ記載・nth/last に無い | 実在 | 両ページの「意味」に `of: w` を追記 |
| F | kyureki「§1 の完全定義を再掲」が実際は縮小版・`kyuMonth` の実行検証ゼロ | 実在 | 文言を「必要部分を再掲」に修正＋ **`kyuMonth`（閏六月）の doctest を追加**（実行検証が立った） |
| G | 非日本語読者はサイトで README の先に進めない（en/ はスタブ 1 枚） | 方針どおり | 緩和策: README 冒頭に「深部は日本語・英語入口は README と llms.txt・要望は Issue へ」の引用ブロックを明示（末尾の重複 Note は削除）。en/ 拡充は従来方針（順次）を維持 |
| H | 註釈の端点表示が `T00:00` と日付裸で不統一 | 実在（原因は表示でなく**閉端→半開端変換の ε＝+1ms**） | impl の表示層で ε を正規化（`fromMs`/`toMs` は正確なまま＝「判定は外部」の器は不変）。`#~>` 期待値を全ブロック機械再生成 |
| I | rebase.md 本文に不可視の SOFT HYPHEN（U+00AD） | 実在（Python で確認・ugrep は拾えず） | 除去。**検査 9 を常設** |

## 中小の指摘と処置

- **3.2 snapTo の隙間挙動が未記載** → 実装の三分岐（被覆域内の隙間＝静的エラー・範囲外＝落として註釈・
  地平線＝警告）を落とし穴に明文化。
- **3.3 綻びログの件数表記**（F1〜F91→F97・F92 の孤児行）→ 40-examples/README と 90-findings の見出しを修正。
- **3.4 未定義束縛 `sekkiTable`＋廃止語 labelOf のコメント**（03-astronomical §3.3）→ §3.1 の
  `sekkiDays`＋`sekkiW`（束縛名射影）の正準形に書き換え・引用形 `"立春"` に統一（4.3 も同時解消）。
- **3.5 `yearEnd` の非対称** → 注記で明文化（追加は語彙裁定事項なので採らず。必要なら `year |> last` の一行）。
- **3.6 小数秒の表示規約が未記載** → reference/README の doctest 規約に「表示は秒未満切り捨て・値は ms 精度」を追記。
- **2.6 RC5 の日付表記ゆれ** → README 英日を「2026-07-08 宣言・追補 07-09」に統一（spec/README と整合）。
- **4.2 未引用の `source:` 文字列**（ADR-32 では字句エラー相当）→ 4 箇所に引用符（03-astronomical×3・
  01-jp-holidays・draft socket 例）。
- **4.4 draft の作業メモ残置**（「タスク2で…」）→ 本文に畳んだ。
- **4.5 クロス参照の接頭辞ずれ・過剰括弧** → gregorian.md（§4.9→spec §4.9・90-open-questions のパス）・
  isOpen.md（40-examples/06 をリンク化）・grid.md（幅規約の入れ子括弧を平文化）・coincides.md
  （シグネチャを `点 -> Bool` に統一）。

## 見送り・据え置き（理由つき）

- **1.3 reference 全ページの引数表統一**・**1.5 gregorian.md の実行例増強**・**1.6 各章冒頭の平易な導入**:
  価値は認める。編集規模が大きく機械検証で守れない種類のため、単発でなく次の文書改訂の区切りで扱う。
- **G の本体（en/ ミラー拡充）**: 従来方針（日本語が正・順次拡充）を維持。
- **メタ表記の揺れ（1.4 の「正体」「形」ラベル）**: nonWorking＝正体・table-literal＝形は意味的に
  意図した使い分けのため不変（射影のシグネチャ表記だけ統一した）。

## 再発防止（doc-consistency 検査 7〜9・npm test 常設）

7. **「残る仮称」の語数主張の横断一致**（現在形文書＋draft・open-questions・INDEX・llms.txt——
   shiftBoundary を含み「二語」等を含まない）＝指摘 A の型。
8. **テスト数の入口間一致**（README 英日・llms.txt・en/spec——値の相互一致を検査。実測値への更新は
   従来どおり区切りごとに手動）＝指摘 B の型。
9. **不可視文字の混入検査**（U+00AD・ゼロ幅系。現在形文書＋design 再帰＋llms.txt）＝指摘 I の型。
