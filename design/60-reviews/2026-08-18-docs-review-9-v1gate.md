# 外部レビュー第 9 回（1.0 公開前ゲート）——指摘と処置の全件表

- **受領**: 2026-08-18（設計者経由・原文は設計者の手元）。処置＝同日。
- **対象**: 本体公開 HEAD `53510bd`（第 8 回 `44493ad` から 15 コミット・約 4,850 行＝take・
  ADR-48〜50・recipes 層新設・英語版 Playground・CLI 拡張・40-examples 09/10/11・564 テスト）＋
  検定 `2c931f3`（変更なし）。
- **方法**（レビュー側申告）: 新規コンテンツの全数精査（並行精査＝MEDIUM 全件を突合・ソース裏取り）・
  **検定の実行検証ブロック 34 件を更新後の実装で再実走（34/34 通過＝意味論の後方互換を実走で証明）**・
  独立機械サニティ（リンク 182 ファイル・不可視文字・en≡ja 同一性・source_sha・Playground 指紋の
  独立再計算）・本体スイート実走（564/564）・1.0 宣言条件と入口状態の突合。レシピの計算根拠
  （Easter・4-4-5・15W・タミル/ヒジュラ暦）も**全て独立再計算で正**。
- **総評**（レビュー側）: 「**1.0 公開のブロッカーはありません（条件付き GO）**。意味論・主張・
  機械検証の三層すべてで矛盾は見つからず。大型追加が既存の規律——doctest・source_sha・指紋・
  横断主張検査——の枠に正しく収まっている」。状態表記の横断一貫（RC5・追補 12・564×4 入口・
  ADR-50×7 箇所・仮称ゼロ・「1.0 を先取りする表記ゼロ」・タグ未打ち）も全数確認済み。
- **当方総括**: M1 は**検証録 11 の追記型運用の穴**——§11.5 の後日談（ADR-49 で take 導入）を
  正しく追記した一方、同ページ上流のカタログ表・§11.4・§11.6 の当初判定を現在形のまま残して
  いた（spec §1.2 は打ち消し線で解消済みなのに、1.0 の看板ページだけ取り残し）。「追記は
  したが当初記述に前方参照を張っていない」型で、同型の点検を M1 処置時に 3 箇所（表・§11.4・
  §11.6）へ展開した。M2 は「ファイル実在検査では捕まらない別ページへの誤リンク」の初例
  （within→combinators）。宣言時チェックリストの補足提案は第 3 回指摘 B（テスト数の入口間
  不一致）と同型の「多入口一斉更新」であり、70-release 作業一覧 2 へ追記した。

## 指摘と処置

| # | 重要度 | 指摘 | 判定 | 処置（2026-08-18） |
|---|---|---|---|---|
| M1 | MED | 検証録 11 の内部矛盾——カタログ行 29 が「射程外」のまま・§11.6 が「A 22・B 7・C 3」のまま。同ページの §11.5 追記（ADR-49 で語彙へ移動・C は 2 項に）と食い違い。正しくは A 23・B 7・C 2。en ミラーも同一矛盾を忠実に複製 | 正当 | カタログ行 29 の判定を「○語彙（`take(n, from:)`＝ADR-49。初版判定は射程外→§11.5）」へ・**§11.4 も同型と点検で判明**（「32 項目中 22」・(C) の 3 行表）——(A) を 23 に・(C) を「初版 3→現在 2」に改め「先頭 N」行は打ち消し線＋移動先明記（spec §1.2 と同じ作法）・§11.6 を「A 23・B 7・C 2（初版 A 22・C 3）」へ。en ミラー全箇所追従・source_sha 更新 |
| M2 | MED | レシピのリンク先違い——cron-last-day-of-month の語彙リンク `within` が `combinators.md`（within への言及ゼロの別実在ページ）を指す。ファイル実在検査では捕まらない型 | 正当 | ja・en とも `within.md` へ修正（レビューは en:55 を指摘・ja 側も同一誤りと確認して両修正） |
| M3 | MED | `reference/strideBy.md` の「二演算子」残存——追補 12 でストライド族は take を加えた三演算子に改編・spec §4.7 は更新済みなのに、strideBy 本文だけ「二演算子に割られている（spec §4.7）」のまま＝引用先の仕様と直接矛盾。en 同 | 正当 | 「[`take`](take.md) とともにストライド族三演算子に割られている（spec §4.7・ADR-38/49）」へ改稿・en 追従 |
| L1 | LOW | 録 11 §(l)「hour 窓は標準に無い」に当時表記マーカー欠落（ADR-50 で標準化済み） | 正当 | 「実測当時の標準に無かった（本節の実測が需要根拠となり ADR-50 で標準化＝§11.5。以下は grid 派生の実演として当時のまま残す）」へ——コードは今も有効な派生実演のため本体は不変 |
| L2 | LOW | stdlib hour の落とし穴 3「分未満が要るときは `grid 1m`」の語の綾（1m で見えるのは分粒度。分未満なら 1s） | 正当 | 「時未満の粒度（分・秒）が要るときは同型の派生 1 行（`minuteW = chronos grid 1m`・秒なら `grid 1s`）」へ・en 追従 |
| L3 | LOW | 40-examples/README 証跡表の doctest 行「05〜09・11」——10（代替暦）が漏れ | 正当 | 10 の doctest 3 ブロックを実地確認のうえ「05〜11」へ・説明セルに「代替暦の算術形」を補完・en 追従 |
| L4 | LOW | glossary「実効パーティション」が §6.5（記号・接頭辞・名前付き引数）に混入——概念用語であり §6.3（暦法と窓）が適所 | 正当 | §6.3 の「区間列型窓」直後へ移動（パーティション型・区間列型と並ぶ位置）・en 同 |
| L5 | LOW | llms.txt「the studies themselves are Japanese」——録 11 は英訳済みで不正確 | 正当 | 「the studies are Japanese except no. 11, which is fully mirrored in English」へ |
| L6 | LOW | en/recipes/README.md だけ「Translated from…」定型句が欠落（他の en ミラー全ページは冒頭に持つ） | 正当 | 定型句を追加（source_sha 注記込み・他ページと同文形） |
| L7 | LOW | 4-4-5 レシピの「各ページの注記」約束（segmentBy 正準形と split 形の関係）に対し segmentBy.md 側の注記が未設置（split 側は weekPart doctest あり） | 正当 | segmentBy.md 関連節に実効パーティション（ADR-48）の受理注記＋ 4-4-5 レシピと split 側 doctest への参照を新設・en 追従 |
| L8 | LOW | recipes 層への逆リンクが reference/README に無い（到達性は README/llms.txt 経由で確保済み・片方向は意図か） | 正当（片方向は意図でなく漏れ） | reference/README 冒頭に「要求から引くときはレシピ集」の 1 行を追加・en 追従 |
| 提案 | — | 宣言時作業一覧 2 の対象が spec/README と CHANGELOG のみ——実際は README 英日・llms.txt・en/spec/README・検定側「RC5 準拠」も同時更新が必要（第 3 回指摘 B と同型の多入口一斉更新）。doc-consistency の「RC5」残存検査への切り替えを宣言コミットに同梱すると構造的に防げる | 採用 | 70-release 作業一覧 2 へ追記（全入口の列挙＋「RC5」残存検査を宣言コミットに同梱する規律） |
| 確認 | — | 旧 URL `reference/shiftBoundary.html` の転送目視（レビュー側はアクセス不可・ソース上の定義は確認済み） | 当方実測で消化 | 実測＝canonical・meta refresh とも `rephase.html` を正しく指す（jekyll-redirect-from の仕様どおり HTTP 200 の転送ページ）。転送先も 200 |

## 処置後の状態

- 564 テスト全通過（doctest 数不変＝文書修正のみ）。en ミラー 8 ファイルの source_sha 追従・
  kramdown/裸パイプ/到達性/フェンス同一性の全機械検査 green。
- レビューの宣言前推奨 3 件（M1〜M3）＋ LOW 8 件＋チェックリスト提案＋残確認 1 件＝**全件同日消化**。
- 1.0 ゲート判定「条件付き GO」の条件は充足——残るは 70-release の記録どおり**条件 2
  （処暑 8/23 第二部→台帳第二期の検証）と宣言時の機械作業のみ**。
