# eol 変換を全面無効化（コミットされたバイト列＝LF のまま checkout する）。
# doctest・検証スナップショットは出力とのバイト一致で照合するため、Windows の
# autocrlf 変換が入ると期待値側だけ CRLF になり偽破損する（CI マトリクスの前提）。
# 利用者が CRLF で書く .kairos は lexer が \r を空白扱いで受ける（別の話・対応済み）。
* -text
