// Kairos Playground (English UI) — the engine lives in /playground/core.js, shared with the
// Japanese page. Evaluator output (annotations/errors) stays in canonical Japanese; see index.html.
import { init } from '../../playground/core.js';
init('en');
